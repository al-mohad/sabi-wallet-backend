# .env.example - Sabi Wallet Backend
# Duplicate this file as .env and fill in your secrets.
# NEVER commit .env to version control.

# -- APPLICATION --
# Log level can be: TRACE, DEBUG, INFO, WARN, ERROR
RUST_LOG=info,sabi_wallet_backend=debug
# Can be `dev` or `prod`
APP_ENV=dev
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
# Secret key for signing tokens and other sensitive data. Generate a strong random key.
APP_SECRET_KEY=generate_a_strong_secret_key_for_jwt_and_other_things

# -- DATABASE --
# Use SQLite for local dev, PostgreSQL for prod.
# SQLite example:
DATABASE_URL=sqlite:sabi_wallet.db
# PostgreSQL example:
# DATABASE_URL=postgres://user:password@localhost:5432/sabi_wallet

# -- REDIS --
# For USSD sessioning, rate limiting, and caching
REDIS_URL=redis://127.0.0.1:6379

# -- SENTRY --
# For error tracking and performance monitoring
SENTRY_DSN=""
SENTRY_ENV=dev

# -- NOSTR --
# The private key of the Nostr keypair used by the backend for coordination.
# THIS IS HIGHLY SENSITIVE.
SABI_NOSTR_NSEC=nsec1...
# Comma-separated list of relays to connect to
NOSTR_RELAYS=wss://relay.damus.io,wss://relay.primal.net,wss://nostr.wine

# -- BREEZ SDK --
# Your Breez API key.
BREEZ_API_KEY=...
# The mnemonic for the Breez wallet.
# THIS IS HIGHLY SENSITIVE.
BREEZ_MNEMONIC="..."

# -- PAYSTACK --
# Your Paystack secret key for verifying webhooks.
PAYSTACK_SECRET_KEY=sk_test_...

# -- AFRICA'S TALKING (for USSD) --
# Your Africa's Talking API key and username.
AT_API_KEY=...
AT_USERNAME=sandbox

# -- ADMIN --
# Default password for the admin user.
# Will be hashed on first startup if no admin exists.
DEFAULT_ADMIN_PASSWORD=SabiAdminPassword123!

# -- LETTRE (Email for Admin Alerts) --
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_HOST=
SMTP_PORT=587
ALERT_EMAIL_FROM="Sabi Wallet Alerts <noreply@sabi.money>"
ADMIN_EMAIL_RECIPIENT=admin@sabi.money
